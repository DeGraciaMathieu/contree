{
  "name": "contree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "npx serve .",
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
